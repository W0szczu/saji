let counter = 0; // Zmienna licznika o wartości początkowej 0
const step = 5; // Stała wartość inkrementacji

do {
    console.log("Licznik: ", counter);
    counter += step;
} while (counter <= 100);

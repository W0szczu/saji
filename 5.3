const myWork = []; 

for (let i = 1; i <= 10; i++) {
    const lesson = {
        name: `Lekcja ${i}`,
        status: i % 2 === 0 ? true : false 
    };
    myWork.push(lesson); 
}

console.log(myWork);
